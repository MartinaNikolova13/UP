#include <iostream>

int main()
{
	int n;
	std::cin >> n;
	int number;
	int biggestNumber=0;
	for (int i = 0; i < n; i++) {
		std::cin >> number;
		if (number > biggestNumber && number < 0) {
			biggestNumber = number;
		}
	}
	std::cout << number;
}
